# Project name used for resource naming
project_name = "app-mod-agent"

# Your Production Google Cloud project id
prod_project_id = "agents-prod-464708"

# Your Staging / Test Google Cloud project id
staging_project_id = "agents-stg"

# Your Google Cloud project ID that will be used to host the Cloud Build pipelines.
cicd_runner_project_id = "agents-stg"

# Name of the host connection you created in Cloud Build
host_connection_name = "google-github"

# Name of the repository you added to Cloud Build
repository_name = "arjunvijaygoogle-delivery-app-mod-agent"

# The Google Cloud region you will use to deploy the infrastructure
region = "us-central1"
