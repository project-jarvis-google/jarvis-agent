
steps:
  - name: 'ghcr.io/astral-sh/uv:debian'
    entrypoint: 'bash'
    args:
      - '-c'
      - |
        make install
        make lint-codespell
    dir: 'agent-app'
    id: 'codespell-check'

options:
  logging: CLOUD_LOGGING_ONLY
